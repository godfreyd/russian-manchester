// const _ = require('lodash');

// module.exports = async (req, res, next) => {
//     req.queryHelper = {
//         getArray: name => {
//             const value = _.defaultTo(req.query[name], []);

//             return Array.isArray(value) ? value : [value];
//         }
//     };

//     await next();
// };
